/*
import {PropertyAllowed} from '../dto/PropertyAccessDto/PropertyAccessDto'
import {propertyAccessEntity} from '../entities/propertyAccessEntity'

export class PropertiesAcccessMapper {
  static map(port: PropertyAllowed[]): propertyAccessEntity[] {
    return port.map(hotel => this.transform(hotel))
  }

  static transform(property: PropertyAllowed) {
    return {
      hotel: property._hotel,
      operaId: property._operaId
    }
  }
}
*/
