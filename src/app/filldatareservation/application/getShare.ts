/**
 * @description Se obtiene el valor de un input, pero se desconoce el origen o de donde viene el valor y que significa 💀
 * @code $(String.format("#TxtstShareName{0}", Index)).val();
 */
export function getShare(index: number) {
  return ''
}
