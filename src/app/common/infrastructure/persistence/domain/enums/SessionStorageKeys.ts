/**
 * @SessionStorageKeys
 * Group the keys and values stored in the session storage
 */

export enum SessionStorageKeys {
  TOKEN = "auth.token",
  USER_IFORMATION = "auth.user",
  PROFILE = "auth.profile",
}