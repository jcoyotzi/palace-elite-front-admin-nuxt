/**
 * @StorageKeys
 * Group the keys and values stored in the local storage
 */
export enum StorageKeys {
  TOKEN = "auth.token",
  USER_IFORMATION = "auth.user",
  PROFILE = "auth.profile",
}